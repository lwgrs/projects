[build]
	publish = "_site"
	command = "npm run build && ls -la .cache"

[build.environment]
ELEVENTY_CACHE_DIR = ".cache"

[[plugins]]
  package = "netlify-plugin-cache"

[plugins.inputs]
paths = [".cache"]